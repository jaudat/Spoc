CREATE SCHEMA PUBLIC AUTHORIZATION DBA
CREATE MEMORY TABLE USERS(USERNAME VARCHAR_IGNORECASE(50) NOT NULL PRIMARY KEY,PASSWORD VARCHAR_IGNORECASE(50) NOT NULL,ENABLED BOOLEAN NOT NULL)
CREATE MEMORY TABLE AUTHORITIES(USERNAME VARCHAR_IGNORECASE(50) NOT NULL,AUTHORITY VARCHAR_IGNORECASE(50) NOT NULL,CONSTRAINT FK_AUTHORITIES_USERS FOREIGN KEY(USERNAME) REFERENCES USERS(USERNAME))
CREATE UNIQUE INDEX IX_AUTH_USERNAME ON AUTHORITIES(USERNAME,AUTHORITY)
CREATE MEMORY TABLE GREETING(ID INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1) NOT NULL PRIMARY KEY,GREETING_DATE TIMESTAMP,GREETING_TEXT VARCHAR_IGNORECASE(255))
ALTER TABLE GREETING ALTER COLUMN ID RESTART WITH 1
CREATE USER SA PASSWORD ""
GRANT DBA TO SA
SET WRITE_DELAY 10
SET SCHEMA PUBLIC
INSERT INTO USERS VALUES('alice','wonderland',TRUE)
INSERT INTO USERS VALUES('billy','bigcow',TRUE)
INSERT INTO USERS VALUES('jane','fatcat',TRUE)
INSERT INTO AUTHORITIES VALUES('jane','ROLE_USER')
INSERT INTO AUTHORITIES VALUES('jane','ROLE_ADMIN')
INSERT INTO AUTHORITIES VALUES('billy','ROLE_USER')
INSERT INTO AUTHORITIES VALUES('alice','ROLE_USER')
